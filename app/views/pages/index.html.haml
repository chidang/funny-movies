- if @movies.count > 0
  - @movies.each do |movie|
    .row.mt-4
      .col-md-5.embed-responsive.embed-responsive-16by9
        %iframe.embed-responsive-item{ src: "https://www.youtube.com/embed/#{movie.youtube_video_id}?rel=0" }
      .col-md-7
        %h4.text-danger= movie.title
        %p= "Shared by: #{movie.user_email}"
        %p
          %strong Description
        %p=movie.description
- else
  %p Currently, don't have any movies available. Please Login/Register and Share a Youtube video.
